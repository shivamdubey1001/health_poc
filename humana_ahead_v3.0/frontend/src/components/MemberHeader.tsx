import type { Member } from '../types/api'
export function MemberHeader({member}:{member:Member}){return <div className="card mb-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-xl font-semibold">{member.name}</h2><span className="rounded-full border border-line px-2.5 py-1 text-xs font-medium">{member.member_id}</span><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700">{member.coverage_status}</span></div><p className="mt-2 text-sm text-muted">{member.plan_name} · Age {member.age_band} · {member.county} County · Preferred: {member.preferred_contact_channel}</p></div><div className="text-sm text-muted"><span className="font-medium text-ink">Claims:</span> {member.claim_count}<br/><span className="font-medium text-ink">Last advocate call:</span> {member.last_advocate_call ? new Date(member.last_advocate_call).toLocaleDateString() : 'None'}</div></div></div>}
