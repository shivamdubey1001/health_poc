import { useEffect,useState } from 'react'; import { BrainCircuit, CircleDollarSign, Clock3, PhoneOff, Workflow } from 'lucide-react'; import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'; import { api } from '../services/api'; import { PageHeader } from '../components/PageHeader'; import { MetricCard } from '../components/MetricCard'; import { LoadingState } from '../components/LoadingState'; import { ErrorState } from '../components/ErrorState'
export default function ImpactPage(){const [d,setD]=useState<any>();const [err,setErr]=useState('');useEffect(()=>{api.cost().then(setD).catch(e=>setErr(e.message))},[]);if(err)return <ErrorState message={err}/>;if(!d)return <LoadingState/>;return <><PageHeader title="Impact & Cost" subtitle="The product is designed as a funnel, not an always-on agent swarm. These numbers are prototype assumptions and session-level AI estimates."/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Agent 1 calls" value={d.agent1_calls} Icon={BrainCircuit}/><MetricCard label="Agent 2 calls" value={d.agent2_calls} Icon={Workflow}/><MetricCard label="Estimated AI cost" value={`$${d.estimated_ai_cost.toFixed(4)}`} Icon={CircleDollarSign}/><MetricCard label="Avg tokens / AI call" value={Math.round(d.average_tokens_per_call)} /><MetricCard label="Potential calls avoided" value={d.potential_calls_avoided} Icon={PhoneOff}/><MetricCard label="Handle-time minutes saved" value={d.estimated_handle_time_minutes_saved} Icon={Clock3}/><MetricCard label="Illustrative advocate time value" value={`$${d.estimated_advocate_time_value.toFixed(2)}`}/><MetricCard label="Illustrative net value" value={`$${d.estimated_net_operational_value.toFixed(2)}`}/></div><div className="mt-6 grid gap-5 xl:grid-cols-5"><section className="card xl:col-span-3"><p className="eyebrow">Compute funnel</p><h2 className="section-title mt-1">Deep agent work occurs on a small subset</h2><div className="mt-5 h-72"><ResponsiveContainer width="100%" height="100%"><BarChart data={d.funnel} layout="vertical"><CartesianGrid strokeDasharray="3 3" horizontal={false}/><XAxis type="number"/><YAxis type="category" dataKey="label" width={150} tick={{fontSize:12}}/><Tooltip/><Bar dataKey="value" fill="#0f6c78" radius={[0,8,8,0]}/></BarChart></ResponsiveContainer></div></section><section className="card xl:col-span-2"><p className="eyebrow">Unit-economics guardrails</p><h2 className="section-title mt-1">What a PM should watch</h2><dl className="mt-5 space-y-4 text-sm"><div><dt className="text-muted">Cost / member evaluated</dt><dd className="mt-1 text-lg font-semibold">${d.cost_per_member_evaluated.toFixed(4)}</dd></div><div><dt className="text-muted">Transcript fallbacks</dt><dd className="mt-1 text-lg font-semibold">{d.transcript_tool_invocations}</dd></div><div><dt className="text-muted">Average AI latency</dt><dd className="mt-1 text-lg font-semibold">{d.average_latency_ms} ms</dd></div></dl><div className="mt-5 rounded-xl bg-panel p-4 text-xs leading-5 text-muted">{d.prototype_assumption}<br/>{d.pricing_note}</div></section></div></>}
